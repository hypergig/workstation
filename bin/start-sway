#! /usr/bin/env bash
set -euo pipefail

logDir="${HOME}/.logs"
mkdir -p "${logDir}"
log="${logDir}/sway.log"

exec &>> "${log}"

echo '----------------------------------------------------------------------------------------------------'
export _JAVA_AWT_WM_NONREPARENTING=1
date
echo 'starting sway'
echo

exec sway
